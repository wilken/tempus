services:
  app:
    image: ghcr.io/wilken/tempus:latest
    build: .
    ports:
      - "80:8080"
    env_file:
      - .env.docker
    environment:
      DATABASE_PATH: /storage/tempus.db
    volumes:
      - storage:/storage
    restart: unless-stopped

volumes:
  storage:
